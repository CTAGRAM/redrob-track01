# IMPORTANT: the ranking step (rank.py) uses ONLY the Python standard library.
# It needs none of the packages below — `python rank.py` runs on a bare CPU-only
# Python 3.9+ with no install, no network, and no GPU. That is the Stage-3
# reproduction path.
#
# The single dependency here is for the OPTIONAL interactive demo (sandbox/app.py)
# and exists so hosted sandboxes (Streamlit Community Cloud / Hugging Face Spaces)
# install it automatically.
streamlit==1.39.0
