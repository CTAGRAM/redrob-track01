# Dependencies for the interactive demo only (sandbox/app.py).
# The ranking engine itself needs none of these.
# Unpinned so the host picks a build with prebuilt wheels for its Python version.
streamlit>=1.40
